import React from 'react';
import FormItem from './formItem';
import SubmitButton from './submitButton';

const Form = () => {
  return (
    <form className="form">
      <FormItem />
      <SubmitButton />
    </form>
  );
};

export default Form;
