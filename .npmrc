# 这三项对于解决 Storybook 依赖解析问题至关重要，一定要保留
node-linker=hoisted            # 使用提升的依赖模式，让依赖结构更接近 npm/yarn，提高兼容性
shamefully-hoist=true          # 将所有依赖提升到顶层，解决 Storybook 等工具的依赖解析问题
strict-peer-dependencies=false # 允许安装不完全匹配的 peer dependencies，避免安装失败

# 对于团队协作有用，确保所有开发者使用相同的 Node 环境
engine-strict=true             # 严格检查 Node.js 版本要求

# 选择 Node.js LTS 版本
use-node-version=20.19.1       # 指定项目使用的 Node.js 版本
